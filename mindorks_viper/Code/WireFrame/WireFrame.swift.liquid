//
//  {{ prefix }}{{ module_info.name }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

class {{ module_info.name }}WireFrame: {{ module_info.name }}WireFrameProtocol {
    
    fileprivate class func setupView(view: {{ module_info.name }}View) {
    	let displayData = {{ module_info.name }}DisplayDataManager()
        let presenter: {{ module_info.name }}PresenterProtocol & {{ module_info.name }}InteractorOutputProtocol = {{ module_info.name }}Presenter()
        let interactor: {{ module_info.name }}InteractorInputProtocol = {{ module_info.name }}Interactor()
        let wireFrame: {{ module_info.name }}WireFrameProtocol = {{ module_info.name }}WireFrame()
        
        view.presenter = presenter
        view.displayData = displayData

        presenter.view = view
        presenter.wireFrame = wireFrame
        presenter.interactor = interactor

        interactor.presenter = presenter
    }
    
    class func create{{ module_info.name }}Module() -> UIViewController {
        // TODO: Instantiate and setup {{ module_info.name }}View here 
    }
    
}

// TODO: Remove if this module doesn't have its own Storyboard
extension UIStoryboard {
    class var {{ module_info.name }}: UIStoryboard {
        return UIStoryboard(name: "{{ module_info.name }}", bundle: nil)
    }
}